//características são propriedades e ações sao chamdas e métodos (funções)

//criando um objeto literal

let dog = {
    name:'Astor',
    latido: function(){
        return 'auauauaua'
    }
}

console.log(`O nome do cachorro é  ${dog.name} e ele faz ${dog.latido()}`)

//console.log(`Áté aqui o objeto cat possui as seguintes propriedades e valores: ${cat.name}, ${cat.age}
 //   ${cat.color}, ${cat.bestFriends[1]}, ${cat.sound()}`)
